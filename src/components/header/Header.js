import React from 'react';
import HeaderList from './headerList/HeaderList';

const Header = () => {
    return (
        <header>
            <h1>Header</h1>
            <HeaderList/>
        </header>
    );
};

export default Header;